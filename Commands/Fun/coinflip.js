export const coinflip = async (sock, m, args) => {
    const result = Math.random() < 0.5 ? "heads" : "tails";
    const emoji = result === "heads" ? "👑" : "🦅";

    return `╔══════════════════════════════════╗
║   🪙 *ℂ𝕆𝕀ℕ 𝔽𝕃𝕀ℙ* 🪙               ║
╚══════════════════════════════════╝

🪙 *Flipping...*

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
${emoji} *${result.toUpperCase()}!* ${emoji}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

_Flip again?_ 🔄`;
};
